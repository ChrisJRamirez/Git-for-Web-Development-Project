hello world!

1. What is Semantic HTML? correct html that not only gives the appeareance for the website, but also helps SEO and helps articulate the meaning of content on a webpage
    2. What is HTML used for? html is used to build the basic structure of a website
    3. What is an attribute and where do we put it? a modifier of an html element that provides additional information, they are put in the start tag
    4. What is the h1 tag used for? How many times should I use it on a page? the h1 tag is to display the main header and also helps with SEO, we should only have 1 h1 on a page
    5. Name two tags that have required attributes - <a> and <img>
    6. What do we put in the head of our HTML document? we link the stylesheet for CSS
    7. What is an id? title that can only apply to one element and always begins with #
    8. What elements can I add an id to? any element
    9. How many times can I use the same id on a page? only once
    10. What is a class? an identifier for one or multiple elements
    11. What elements can I add a class to? any element
    12. How many times can I use the same class on a page? as many times as you would like
    13. How do I get my link to open in a new tab? anchor tags
    14. What is the alt attribute in the image tag used for? to display an alternative image in case the primary img cannot be shown, also is used for screen readers
    15. How do I reference an id? with a # and the name of the id
    16. What is the difference between a section and a div - a div is an in line element while section is a block level element that groups its content
    17. What is CSS used for? adding style to webpage
    18. How to we select an element? Example - every h2 on the page - we would just put   h2 {}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one - a class can be used multiple times, example being if you are talking about different plants that all fall under a "perennial" class
		an id can only be used once and would apply to something like a driver's license number
    20. How do we select classes in CSS?  .class
    21. How do we select a p element with a single class of “human””? p.human {}
    22. What is a parent child selector? When would this be useful? selects all direct children of the specified element. useful in making all children elements the same
    23. How do you select all links within a div with the class of sidebar? div.sidebar link
    24. What is a pseudo selector? used to define a special aspect of an element such as :hover
    25. What do we use the change the spacing between lines? line-height:
    26. What do we use to change the spacing between letters? letter-spacing:
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform  uppercase, lowercase, capitalize
    28. How do I add a 1px border around my div that is dotted and black? border: 1px dotted black;
    29. How do I select everything on the page? with the universal selcector * {}
    30. How do I write a comment in CSS? /* comment */
    31. How do I find out what file I am in, when I am using the command line? cd -> enter
    32. Using the command line - how do I see a list of files/folders in my current folder? ls -al
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm (remove), have to be careful cuz if you are not careful about you are selcting to remove you could wipe your computer
    34. Why should I use version control? helps maintain history of changes and allows you to go back in time in case you delete or mess up a file
    35. How often should I commit to github? once youre done with your project
    36. What is the command we would use to push our repo up to github? git push
    37. Walk me through Lambda's git flow. git helps Lambda students work on projects from the cloud simultaneously as well as individually. We can grab a file that our teacher built and fork it to make it our own/not be editing the teachers copy
		we can also use it to work on a proejct with our track teams, while also maintaining an individuality because of the history of any changes we have made to the code/project. Along with this we can go back to old revisions.
		we can clone a repository from a hosting platform like Github that our teacher posted,  we can fork it to make it our own, we cna then make our own branch to isolate our work, once we are comfortable/want to save to local repository (our computer) we can commmit
		after we commit we can push our local file to the remote repository  (github) for our teachers/fellow students to view/edit and make their own clones

Stretch Questions

    1. What is the difference between an inline element and a block element? block level elements width is defined by its parent
    2. What happens when an element is positioned absolutely? it sits on its own line outside the scope of the rest of the page
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
